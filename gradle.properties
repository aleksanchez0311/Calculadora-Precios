# Recommended Gradle properties for local builds
org.gradle.jvmargs=-Xmx1536m
org.gradle.daemon=true
kotlin.code.style=official

# Optional: set Kotlin jvm target if needed by your build
kotlin.jvm.target=17
